alert("Schedule ended for " + "<%= @client.name%>");
jQuery(<%="client_links_container_#{@client.id}" %>).html("<%= j render partial: 'clients/client_links', locals: { client: @client} %>");
jQuery(<%= "ended_at_cell_#{@client.id}" %>).html("<%= @schedule.end_time %>");